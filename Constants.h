#pragma once

#include <Arduino.h>

const int BRIGHTNESS_STEP = 5;
const int BRIGHTNESS_MIN = 5;
const int BRIGHTNESS_MAX = 255;
const int DMX_UNIVERSE_SIZE = 512;

const int LED_COUNT = (144 * 2); // 144 pixels/m * N meters
